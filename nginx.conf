# worker_processes 1;

# events {
#     worker_connections 1024;
# }

# http {
#     include       mime.types;
#     default_type  application/octet-stream;
#     sendfile        on;
#     client_max_body_size 50M;

#     # Redirect HTTP to HTTPS
#     server {
#         listen 80;
#         server_name observatoriodebarcarena.com.br;  # Replace with your actual domain
        
#         # Allow Let's Encrypt challenges
#         location /.well-known/acme-challenge/ {
#             root /usr/share/nginx/html;
#         }
        
#         # Redirect all other HTTP traffic to HTTPS
#         location / {
#             return 301 https://$server_name$request_uri;
#         }
#     }

#     # HTTPS server
#     server {
#         listen 443 ssl http2;
#         server_name observatoriodebarcarena.com.br;  # Replace with your actual domain

#         # Let's Encrypt SSL Certificate paths
#         ssl_certificate /etc/letsencrypt/live/observatoriodebarcarena.com.br/fullchain.pem;
#         ssl_certificate_key /etc/letsencrypt/live/observatoriodebarcarena.com.br/privkey.pem;
#         ssl_trusted_certificate /etc/letsencrypt/live/observatoriodebarcarena.com.br/chain.pem;

#         # SSL Security Settings (recommended)
#         ssl_protocols TLSv1.2 TLSv1.3;
#         ssl_ciphers HIGH:!aNULL:!MD5;
#         ssl_prefer_server_ciphers on;
#         ssl_session_cache shared:SSL:10m;
#         ssl_session_timeout 10m;

#         # OCSP Stapling
#         ssl_stapling on;
#         ssl_stapling_verify on;

#         root /usr/share/nginx/html;
#         index index.html;

#         location /admin/dimensao/ {
#             try_files $uri $uri/ /index.html;
#             add_header 'Access-Control-Allow-Origin' '*';
#             add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS';
#             add_header 'Access-Control-Allow-Headers' 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range,Authorization';
#         }
        
#         location / {
#             try_files $uri /index.html;
#         }

#         location /api/ {
#             proxy_pass http://backend:8081/;
#             proxy_set_header Host $host;
#             proxy_set_header X-Real-IP $remote_addr;
#             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#             proxy_set_header X-Forwarded-Proto $scheme;
#         }
#     }
# }



 worker_processes 1;
 events {
     worker_connections 1024;
 }
 http {
     include       mime.types;
     default_type  application/octet-stream;
     sendfile        on;
     client_max_body_size 50M;
     server {
         listen 80;
         listen 443 ssl;
         server_name _;
     ssl_certificate /etc/ssl/certs/selfsigned.crt;
     ssl_certificate_key /etc/ssl/private/selfsigned.key;
         root /usr/share/nginx/html;
         index index.html;
         location /admin/dimensao/ {
             # Make sure this route is accessible directly
             try_files $uri $uri/ /index.html;
             # Enable CORS if needed'
             #add_header 'Access-Control-Allow-Origin' '*';
             #add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS';
             #add_header 'Access-Control-Allow-Headers' 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range,Authorization';
         }
         location / {
             try_files $uri /index.html;
         }
         location /api/ {
             proxy_pass http://backend:8081/;
             proxy_set_header Host $host;
         }
     }
 }